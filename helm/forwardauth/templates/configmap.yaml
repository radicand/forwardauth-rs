{{- if not .Values.existingConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "forwardauth.fullname" . }}
  labels:
    {{- include "forwardauth.labels" . | nindent 4 }}
data:
  application.yaml: |
    {{- .Values.applicationYaml | toYaml | nindent 4 }}
{{- end }}
